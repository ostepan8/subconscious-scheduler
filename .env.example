# ── Subconscious AI ──────────────────────────────────────
# Get your API key at https://subconscious.dev/platform
SUBCONSCIOUS_API_KEY=

# ── Shared Secrets ───────────────────────────────────────
# Must match values set in Convex dashboard environment variables
TOOL_ENDPOINT_SECRET=

# ── App URL ──────────────────────────────────────────────
# Your production URL (used for OG images & metadataBase)
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# ── Convex ───────────────────────────────────────────────
# These are set automatically by `npx convex dev`
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CONVEX_SITE_URL=

# Convex Auth (set these in the Convex dashboard, not here)
# Generate with: openssl genpkey -algorithm Ed25519
# JWT_PRIVATE_KEY=  (set in Convex dashboard)
# JWKS=             (set in Convex dashboard)
# SITE_URL=         (set in Convex dashboard — your .convex.site URL)

# ── Notifications — Email (Resend) ──────────────────────
# Get your API key at https://resend.com
RESEND_API_KEY=
RESEND_FROM_EMAIL=subconscious-scheduler@subconscious.dev

# ── Unsubscribe Links ──────────────────────────────────
# Set in Convex dashboard — used for unsubscribe links in emails
# APP_URL=https://scheduler.subconscious.dev
